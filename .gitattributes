# Auto detect text files and perform LF normalization
* text=auto
# Enforce LF line endings for shell scripts and critical files
*.sh text eol=lf
*.bash text eol=lf
Dockerfile text eol=lf
docker-compose.yml text eol=lf